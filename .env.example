SESSION_SECRET=change-me-to-a-long-random-string
DB_PATH=./database/cardoso.db
PORT=3001

# Development only (not needed in production)
# FRONTEND_ORIGIN=http://localhost:5173

# Set to "production" when running as a service
# NODE_ENV=production
