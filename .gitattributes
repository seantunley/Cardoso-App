# Auto detect text files and perform LF normalization
* text=auto

# Binary assets — never apply line-ending conversion
*.pdf binary

# Generated review tracker — pin to LF so `node scripts/review-report.mjs`
# (which writes LF) never churns the committed file on a CRLF checkout.
docs/review/*.md text eol=lf
